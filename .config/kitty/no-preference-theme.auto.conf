# Used automatically by kitty when the OS reports no color scheme preference.
# GNOME reports no-preference whenever "Dark style" is off, so this is the
# light-mode file on that desktop.
include token-light.conf
