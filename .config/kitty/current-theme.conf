## name: Token Dark

# Token colorscheme, classic appearance.
# Only a fallback: the *-theme.auto.conf files follow the OS color scheme and
# their colors override whatever is set here.
include token-dark.conf
